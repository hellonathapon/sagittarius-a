import styled from "styled-components";

export const Header = styled.header`
    ul {
        li {
            list-style: none;
            padding: 0 1rem;
        }
        display: flex;
        justify-content: flex-end;
        width: 100%;
    }
    display: flex;
`